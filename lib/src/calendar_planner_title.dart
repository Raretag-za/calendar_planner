import 'package:flutter/cupertino.dart';

class CalendarPlannerTitle extends StatelessWidget {
  const CalendarPlannerTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

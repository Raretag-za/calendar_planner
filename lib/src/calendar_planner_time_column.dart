import 'package:flutter/cupertino.dart';

class CalendarPlannerTimeColumn extends StatelessWidget {
  const CalendarPlannerTimeColumn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

import 'package:flutter/cupertino.dart';

class CalendarPlannerTask extends StatelessWidget {
  const CalendarPlannerTask({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
